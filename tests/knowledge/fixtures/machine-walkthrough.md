# Example machine

## Initial enumeration

We ran a service scan to establish the exposed attack surface.

```text
80/tcp open http
```

## Web investigation

The scan results led us to inspect the web service, where we found a distinct virtual host.

```text
portal.example.test
```
