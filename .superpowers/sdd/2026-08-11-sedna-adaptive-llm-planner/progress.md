# SDD ledger — plan: docs/superpowers/plans/2026-08-11-sedna-adaptive-llm-planner.md

Baseline: cd6db5f (M6A complete — Tasks 1-9, 1293 tests); M6A handoff verified (public compatibility surface imports from sedna.engagement; all 10 service methods present).
Task 1: complete — schema/execution.py (PlaceholderKind/BindingPolicy, ExecutionPlaceholder con policy TARGET→authorized_scope e SOURCE_CASE_CREDENTIAL→never_auto_bind, ExecutionCondition/PlatformConstraint source-cited, ExecutionExample con placeholder ordinati + coverage esatta template, command 8192 senza control/NUL/flag, platform-in-prose reject, requires_validation=True); drafts.py (DraftExecutionPlaceholder/Condition/PlatformConstraint/Example con policy identiche, SemanticDraftBundle.execution_examples con parent validation reference|case-step e unicità local_id, _segment_indexes esteso); export in schema/__init__, semantic/__init__, knowledge/__init__. 16 focused tests GREEN (64 semantic+drafts+schema); 1309 full-suite tests; Ruff and diff-check clean.
Task 2: in progress
Task 2: complete — materialize.py (MaterializedSemanticContent, materialize_semantic_content, _materialize_artifacts_with_local_ids con mappa local→canonical incl. case-step, _materialize_execution_examples con stable_execution_example_id content-addressed senza draft IDs nel parent, materialize_bundle come wrapper, _all_cited_indexes esteso agli esempi); schema/semantic.py (manifest + execution_example_schema_version/emitted_execution_example_ids con validazione sorted/unique e coerenza, bundle + execution_examples con sorted-unique, ID unici, provenance, parent membership, coverage manifest esatta); prompts.py v2 (esempi solo in execution_examples, parametrizzazione target/credenziali, constraints source-cited); compiler.py (SEMANTIC_SCHEMA_VERSION 2.5.0, COMPILER 9, EXECUTION_EXAMPLE_SCHEMA_VERSION 1, _verified usa materialize_semantic_content, _bundle con esempi + manifest). Test aggiornati (monkeypatch materialize_semantic_content, prompt v2) + 4 nuovi materialize tests (ID deterministici/content-scoped, parent canonical, flag reject). 181 semantic tests GREEN; 1313 full-suite tests; Ruff and diff-check clean.
Task 3: in progress
Task 3: complete — repository.py (load_execution_examples bundle-owned con filtri parent + exact set match e _validate_stable_id; _require_current_retrieval_bundle/quarantine con allowlist legacy esatta 2.4.0/compiler-8/prompt-1 senza example schema e senza esempi, altrimenti stale fail-closed; load_current_semantic_result/semantic_result_is_current con execution_example_schema_version param, richiesto esatto solo quando il manifest ha esempi emessi). Fix: nested_ids non include example_ids (check manifest separato). Test: test_execution_example_migration.py (3 GREEN: legacy strategic-only accettato dalla retrieval + relearn v2 con esempi + unchanged con zero host calls; load_execution_examples parent/exact-set; atomic replacement con write failure → bundle vecchio completo). 1316 full-suite tests; Ruff and diff-check clean.
Task 4: in progress
Task 4: complete — retrieval/models.py (ExecutionExampleLocator/IndexedExecutionExampleState/CoverageCode/CoverageGap/Drilldown; IndexedSourceState + semantic_schema_version/execution_example_schema_version/execution_examples sorted-unique; source_projection_digest include i tre; protocol RetrievalIndex + get_execution_example_locators/get_source_capability); projection.py (SOURCE_PROJECTION_VERSION canonical-projection-v3, project_execution_example_locators ID-only, project_source_state con capability e stati esempi); sqlite.py (schema v5 con execution_example_lookup + execution_example_parent_idx, colonne capability in indexed_sources, insert/delete lookup nella stessa transazione, _REQUIRED_SCHEMA_COLUMNS, _actual_source_state legge capability+esempi, metodi locator/capability, audit coerente); service.py (execution_example_loader optionale + get_execution_examples con revision guard, identità cross-validata, gap legacy_bundle_without_examples da capability senza loader); hades_runtime.py injection di repository.load_execution_examples. Test: 4 nuovi (marker mai searchable, locator ID-only, legacy vs current-no-examples, digest cambia con esempi, drill-down esempi/gap) + aggiornati (schema v5, _IndexDouble, prompt). 172 retrieval tests GREEN; 1321 full-suite tests; Ruff and diff-check clean.
Task 5: complete — planning contracts in src/sedna/planning/{models,ports,__init__}.py plus tests/planning/test_models.py. Closed observation/situation, strict settlement union + JSON adapter, strategy ledger/frontier/critic/audit records, bounded conversion indexes and proof candidate admissions, terminal reconciliation port. Final manual review completed after independent review delegation budget was exhausted: current source inspected against Task 5 plan and all reproducible findings closed with RED→GREEN tests. 31 focused planning tests GREEN; 1352 full suite tests GREEN; Ruff and diff-check clean. Committed as 7cb3147 (feat(planning): define adaptive planner contracts).
Task 7: complete — four-role structured planning LLM boundary in src/sedna/planning/{llm,prompts,__init__}.py plus tests/planning/test_llm.py and test_prompt_injection.py. Four exact purpose/type contracts (observe/plan/critic/repair); frozen extra-forbid event-bound requests; binary-safe base64 JSON payloads; local payload/result revalidation; no provider/model override, JSON mode and temperature 0; closed safe failures including oversized canonical request before host call and malformed host attribution; no raw host audit/response is returned. Static version-1 prompts carry independent IDs and state the untrusted-data boundary. Prompt-injection regression confirms hostile evidence is serialized only in the input JSON (base64) and never interpolated into instructions. Planner request’s archive/retrieval/context fields remain deliberately deferred to their authoritative Task 10–11 surfaces, which do not yet exist. Final manual read-only review found no reproducible Critical/Important issues. 48 focused planning tests GREEN; 1379 full suite tests GREEN; Ruff and diff-check clean. Commit pending.
Task 8: complete — closed 19-member typed planning journal vocabulary in engagement, exact 7 settlement-bookkeeping + 12 active-planning lifecycle partition, inert M6A replay, sealed package-private PlanningEventCommitCapability, and four pure planning converters. Final review found and closed an Important pass-through gap: conversion envelopes no longer accept prebuilt journal payloads; typed allocation/host contexts are cross-validated field-for-field against authoritative ObservationBatchDraft/InterpretationAudit, PlanRequestAudit/PlannerDraft/critic-repair-rejection-gap models, StrategyReconciliation/archive/reactivation transitions, and research policy/source models. Closed dispatch covers all 19 kinds exactly once, resolves preallocated IDs, validates indexes/evidence/proof admission, computes canonical digests locally, deep-revalidates through EventPayload, and enforces payload/batch bounds. Request and research-assessment digest validators fail closed. Public planning surface exports the four converters while conversion contexts remain internal; engagement imports no planning module. Final manual read-only review APPROVED after three bounded fix rounds. 189 focused Task 8 tests GREEN; 1457 full suite tests GREEN; Ruff src/tests and git diff-check clean. Committed in this Task 8 feature commit.
Task 9: complete — deterministic SituationReducer and PlanningService settlement. Event-only replay preserves material vs authority revision, structured observations, hypotheses/unresolved information, research provenance, attachment-scoped interpretation coverage, attempts, private proof/secret locators, legacy reopen invalidation, and pure proof generation/rejection admission seams. Settlement uses state.json only as a validated cache, paginates all descriptors, processes 32-KiB slices with a 64-slice/2-MiB invocation budget, resumes at the first gap, handles zero-byte and unsupported media idempotently, maintains a canonical 512-record pending page with true total/digest/opaque cursor and journal-derived fairness, retries one stale CAS without duplication, returns closed safe failure variants, and reloads/cross-validates terminal reconciliation. Three review/fix rounds plus the final manual boundary review closed partial-slice digest/absolute-offset bugs, mixed-media false-settled, retryable pending reason, >511 local-event batch / >64 interpretation projection bounds, and >64 lifetime slice-event aggregation. Final manual read-only review APPROVED: 51 focused Task 9 tests GREEN; 1509 full-suite tests GREEN; Ruff src/tests and git diff-check clean. Committed as c246786 (feat(planning): settle engagement evidence).
Task 10: complete — fixed-path descriptor-confined `strategy-archive.jsonl` with data-only deep-validated records, canonical streaming JSONL, 100k/64-MiB hard bounds, 256-record pages, deterministic digests/cursors, archive+journal CAS, 0600 mode, symlink/nonregular/corrupt fail-closed reads, crash-safe fsync/replace, and bounded iterator consumption. `StrategyLedgerReducer` replays only complete atomic reconciliation/archive/reactivation batches from Task 8 snapshots; validates identity, ancestry, full reconciliation operations and declared digests; keeps 32 families/64 variants, eight attempts per variant and 256 globally; folds all evicted attempts into cumulative category/count/revision/digest aggregates; exposes deterministic cold partition and six typed reactivation predicates capped at 16/16 KiB. Shared 19-event dispatch proves 15 direct no-ops. Two review/fix rounds closed cumulative-history loss, global hot-attempt overflow, silent partition loss, weak operation validation, missing batch-companion rejection, archive boundary/corruption coverage, and unrelated historical test formatting churn. Final manual review APPROVED: 209 focused repository/service/ledger/situation tests GREEN; 1562 full-suite tests GREEN; Ruff and diff-check clean. Commit message: feat(planning): retain adaptive strategy ledger.
Task 11: complete — situation-conditioned bounded knowledge assembly in `planning/retrieval.py` and planner-only source registry views. Strict vertical TDD covered managed-only 128/64-KiB snapshots and 16/16-KiB hints, preserved parameterless M6A APIs, evidence-backed authorized per-target queries, privacy filtering across every textual lane with symbolic-only credential availability, separated/deduplicated retrieval lanes, 16-example drill-down with applicability checks and 32 typed bounded coverage gaps, deterministic canonical/source revision binding, retrieval-unavailable semantics, and structured research candidates. The prior review FAIL findings were remediated; final focused gate 44 passed, full suite 1582 passed, Ruff check and focused format check clean, git diff-check clean. Independent final review pending immediately before commit.
Task 12: complete — adaptive planner/critic orchestration, one bounded repair, composite cache, event-only sequential frontier replay, optimistic CAS, strategy reconciliation, typed archive/reactivation, and fail-closed semantic grounding. Versioned OrgRun remediations closed every independent-review finding, including 128-KiB result repair, stale prior-frontier return, converter-only typed event batches, research policy, archive/reactivation ledger transitions, zero-score evidence semantics, archive compensation, concurrent journal append, and commit-then-raise durability discrimination. Final dedicated v4 review APPROVED with no findings. Final isolated staged gates: 113 focused Task 12 tests GREEN; 1609 full-suite tests GREEN; Ruff src/tests and git diff-check clean; no tracked .pyc or uv.lock noise. Commit message: feat(planning): orchestrate adaptive frontier.
